#include "main.h"
#include <stdio.h>

/**
 * main - Tests for the positive_or_negative function
 *
 * Return: Always 0
 */
int main(void)
{
    int zero = 0;

    positive_or_negative(zero);

    return (0);
}

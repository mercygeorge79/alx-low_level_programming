#include "main.h"

/**
  * add - adds two integers and returns result
  * @p: int to be added to p
  * @q: int to be added to p
  * Return: sum of p and q
  */
int add(int p, int q)
{
	int s;

	s = p + q;

	return (s);
}
